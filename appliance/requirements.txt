numpy==2.4.3                # For arrays in mocks
numba==0.64.0               # For perf (as tested)
paho-mqtt==1.6.1            # For MQTT communication
scipy==1.17.1               # For scientific computing
pydantic==2.12.5            # For data validation
pydantic-settings==2.13.1   # For settings management
# Add later: torch, networkx, onnxruntime (for TinyML)