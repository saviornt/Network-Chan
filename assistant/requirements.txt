# ───────────────────────────────────────────────────────────────
# Assistant (AIOps Platform) - FastAPI backend dependencies
# Core web framework + async runtime
fastapi>=0.115.0          # latest stable as of 2026; includes Starlette/ASGI
uvicorn>=0.32.0           # ASGI server (with --reload for dev)
gunicorn>=25.1.0          # production WSGI server (optional, but good to have)

# Appliance dashboard
fastapi>=0.135.1
uvicorn>=0.42.0
gunicorn>=25.1.0
python-multipart>=0.0.22
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.5.0
qrcode[pil]>=8.2
pyotp>=2.9.0

# HTTP client (for LLM calls, external integrations if needed)
httpx>=0.27.0             # async HTTP client (better than requests for FastAPI)

# Structured logging & observability
structlog>=25.5.0

# Configuration & validation (consistent with appliance)
pydantic>=2.8
pydantic-settings>=2.3
pyyaml>=6.0.3

# Authentication / security basics
pyjwt>=2.9.0              # JWT for API auth
passlib[bcrypt]>=1.7.4    # password hashing
python-multipart>=0.0.22   # form data support (file uploads, etc.)

# Typing & modern Python
typing-extensions>=4.12.2

# Optional but very useful for AIOps features
tenacity>=9.0.0           # retries/backoff for flaky LLM/MQTT calls
orjson>=3.10.0            # ultra-fast JSON (used by FastAPI for faster responses)